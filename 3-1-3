//하나의 정수를 입력 받아서, 그 수의 제곱의 결과를 출력하는 프로그램을 작성해라.

#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main(void)
{
	int num, multi;//정수형 정의

	printf("정수를 입력하시오: ");
	scanf("%d", &num);//정수 입력받기
	
	multi = num * num;//multi 계산식
	
	printf("%d의 제곱은 %d입니다.\n", num, multi);//결과값 출력

	return 0;
}
